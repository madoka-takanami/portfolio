<%= button_to t('restaurants.index.visited'), restaurant_visits_path(restaurant.id),
                          id: "js-visits-button-#{ restaurant.id }",
                          class: "btn btn-sm btn-secondary",
                          method: :delete,
                          remote: true,
                          data: { "turbolinks" => false}  %>
