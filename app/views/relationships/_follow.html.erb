<%= button_to t('users.index.follow'), user_relationships_path(user.id),
                          class: "btn btn-sm btn-neutral btn-mid",
                          method: :post,
                          remote: true %>
