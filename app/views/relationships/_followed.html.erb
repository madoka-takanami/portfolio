<%= button_to t('users.index.followed'), user_relationship_path(user.id, current_user.id),
                          id: "js-follow-button-#{ user.id }",
                          class: "btn btn-sm btn-secondary btn-mid",
                          method: :delete,
                          data: {"turbolinks" => false},
                          remote: true %>
